- hosts: ec2
  become: true
  tasks:
    - name: Ping localhost
      ping:

    - name : Rollback Nginx
      ansible.builtin.include_tasks:
        file : rollback-provision-steps/rollback-nginx.yml
    



    
